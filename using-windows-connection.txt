How to get X11 forwarding to Windows.
Windows *must* set the DISPLAY manually:

$env:DISPLAY = "127.0.0.1:0.0"
ssh -X boss@192.168.1.142
ssh -v / -vv / -vvv

Passing clipboard back to Windows via xclip and xsel...
